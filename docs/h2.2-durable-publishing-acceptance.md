# H2.2 — Durable Scheduling & Real External Execution — Acceptance Record

Date: 2026-09-22
Status: IMPLEMENTED / LIVE EXTERNAL DELIVERY VALIDATION PENDING
Objective stage: DEMO → BETA
AMH: v0.3-PARTIAL
AMH run: AMH-20260922-PILTOVER-H2-2
Message-delivery execution timeout: 30 minutes

## Implemented

- Database-backed publishing scheduler; browser timers are not authoritative.
- Dedicated server worker: `npm run worker:publishing`.
- Due-job claiming with lease owner/expiry and expired-lease reclaim.
- Durable `PublishingAttempt` ledger for every provider attempt.
- Provider payload fingerprint and request fingerprint.
- Provider response + provider post ID persistence.
- Failure taxonomy: RETRYABLE, RATE_LIMITED, AUTH_EXPIRED, VALIDATION_FAILED, PROVIDER_REJECTED, NETWORK, UNKNOWN.
- Retry-After/exponential backoff/max attempts.
- RETRY_PENDING, BLOCKED, DEAD and UNKNOWN operational states.
- Manual retry with UNKNOWN reconciliation guard.
- Ambiguous after-send outcomes never blind-retry.
- Approval validation remains bound to exact provider payload hash.
- Scheduler respects ToolGrant/approval policy before external writes.
- Queue API exposes job + recent attempt history.
- Audit entries correlate execution, attempt fingerprints, reconciliation and result.

## Verification evidence

- Prisma schema validation: PASS.
- Migration `20260922193000_h2_durable_publishing`: applied.
- Prisma migration status: 25 migrations, database up to date.
- TypeScript `npx tsc --noEmit --pretty false`: PASS.
- H2.2 + vNext publishing tests: 14/14 PASS.
- H2.1 + H2.2 + vNext publishing regression: 18/18 PASS.
- Next.js production build: PASS, exit code 0.
- Production route includes `/api/vnext/publishing/queue`.

## Exit-gate state

1. Scheduled real post executes with browser closed — IMPLEMENTATION PASS; live external-provider proof pending.
2. Restart before due time does not lose job — E2 PASS via persisted job + lease-expiry reclaim; production restart/live-provider proof pending.
3. Retry cannot duplicate provider post — E2 PASS; COMPLETED is stable and UNKNOWN requires reconciliation.
4. Provider post ID and terminal state persist — E2 PASS.
5. Rate-limit/auth-expiry produce recoverable states — E2 PASS.
6. Approval pauses/resumes same logical job — implementation/payload binding PASS; end-to-end UI/live approval proof pending.
7. Audit reconstructs request → approval → attempts → result — implementation PASS; live operator trace pending.

## Live proof still required before DONE

- Real immediate provider publish.
- Real scheduled publish with browser closed.
- Restart backend/worker before due time and observe same persisted job execute.
- Real/controlled transient provider failure and retry.
- Real auth-expiry/reconnect flow.
- Approval-gated real publish.
- Duplicate retry/reconciliation against real provider state.
- Capture provider post IDs and audit evidence.

No live-provider claim is inferred from deterministic tests.
